import { Terminal } from './components/Terminal';

function App() {
  return (
    <div className="min-h-screen bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-gray-900 via-purple-900 to-gray-900">
      <Terminal />
    </div>
  );
}

export default App;
